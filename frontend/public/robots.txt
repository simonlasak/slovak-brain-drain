# Crawling allowed. Opened 27 August 2026, when the English site was complete:
# all four sections, /methodology with its download links, and /resources with
# every external claim fetched and verified.
#
# STILL A WORK IN PROGRESS, and knowingly indexed as one: the Slovak translation
# of every section is a stub, so a visitor who picks SK gets the English text
# behind a notice saying the translation is in preparation. That is the one thing
# an indexed snapshot taken now will misrepresent.
#
# TO CLOSE IT AGAIN: change "Disallow:" below to "Disallow: /". Note that
# de-indexing is slow, so removal takes far longer than the block does.

User-agent: *
Disallow:

# Generated at build time by @astrojs/sitemap from the built routes, so a new page
# cannot be missed by forgetting to list it. /404 is excluded deliberately.
Sitemap: https://slovak-brain-drain.simonlasak4.workers.dev/sitemap-index.xml
