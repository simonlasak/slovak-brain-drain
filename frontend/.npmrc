# visx declares a peer dependency on React <=18 and this project runs React 19.
# Without this, a clean install on CI fails to resolve the tree. package-lock.json
# was generated under the same setting, so npm ci reproduces it exactly.
legacy-peer-deps=true
